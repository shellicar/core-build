import { equal } from 'node:assert/strict';
import { describe, it } from 'node:test';

describe('a test', () => {
  it('should pass', () => {
    equal(true, true);
  });

  it('should pass2', () => {
    equal(true, true);
  });
});
